# Step 1 Data

This folder contains the labeled image data used by the classifier.

- `dataset_dev/`: development split used for feature extraction, validation benchmarking, and final model fitting
- `dataset_test/`: held-out test split used for final evaluation
